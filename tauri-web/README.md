# AriannA — Web / Browser

Pure Vite + TypeScript project. No Tauri. Works in any browser.
Also opens directly in VS Code with the recommended extensions.

```bash
pnpm install
pnpm dev       # http://localhost:1420
pnpm build     # ./dist
```

**VS Code:** open folder → F5 → "AriannA Web (Vite)"

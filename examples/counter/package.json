{
  "name": "arianna-starter-counter",
  "version": "1.0.0",
  "description": "Fine-grained reactivity demo — signals, computed values, two buttons.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "live-server --port=5500 --no-browser",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.5.0"
  }
}
